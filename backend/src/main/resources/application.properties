spring.application.name=backend

# Disable automatic Docker Compose lifecycle integration (prevents startup failure when Docker is not available)
spring.boot.docker.compose.enabled=false

# Allow Render (or other platforms) to set the HTTP port via PORT environment variable
# Defaults to 8080 when PORT is not provided.
server.port=${PORT:8080}

